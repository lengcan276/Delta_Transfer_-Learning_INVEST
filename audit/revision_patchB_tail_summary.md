# Patch B-tail — Final report

Repository HEAD (pre-revision): `3a72bc9`
Branch: `revision-after-phase5`
Date: 2026-05-20
Canonical repository: `https://github.com/lengcan276/INVEST-n13`

## Status

**PASS_PATCH_B_TAIL_UNRESOLVED_ZERO**

## What was fixed

### `26.0`

- **Source**: `paper/main.tex:205` (Data Availability section,
  added in Patch A) and `paper_overleaf/main.tex:205` (mirror).
- **Classification**: PROVENANCE_EXPLANATORY_NUMBER — derived
  arithmetic (2 × 13); not a scientific claim.
- **Treatment**: A (rewrite). Replaced
  `"singlet + triplet = 26 files"` with
  `"singlet and triplet outputs"`. The substantive content (two
  spin channels per molecule) is preserved; the redundant derived
  integer is dropped.

### `0.41`

- **Source**: `paper/main.tex:213` (Data Availability section,
  added in Patch A) and `paper_overleaf/main.tex:213` (mirror).
- **Classification**: PROVENANCE_EXPLANATORY_NUMBER — audit-derived
  ADC(2) parsed-vs-CSV rounding-bound; not a quantum-chemistry result.
- **Treatment**: A (rewrite). Replaced
  `"within $0.41$~meV"` with
  `"within sub-meV rounding-level differences"`. The substantive
  content (rounding-level, not method-level) is preserved; the
  precise audit-derived bound is dropped from the manuscript and
  remains available in `audit/phase5_adc2_rounding_only_audit.md`
  for any reader who wants it.

### `audit_numbers.py` new result

```
exit code: 0
numbers extracted     = 513 (was 515)
non-trivial numbers   = 257 (was 259)
unresolved            = 0   (was 2)
Major checks tripped  = 0 / 7
```

## What did not change

- No raw calculation files changed.
- No scientific numerical values changed (per-molecule numeric
  drift = 0; see `audit/revision_patchB_tail_numeric_safety.md`).
- No molecule classifications changed.
- No data/source/ or data/processed/ touched.
- No `results/canonical_metrics.json` hand-edited; it was
  regenerated by `scripts/99_emit_canonical.py` in the Patch B
  review-fix and was not touched in Patch B-tail.
- No `results/scscc2_extension_n13/cross_check_n13.csv` touched.
- No `results/scscc2_extension_n13/stats_n13.json` touched.
- No `figures/caption_data/*.json` touched.
- Patch C was not started.
- No git commit or push.
- No network, no ssh, no rsync, no scp.

## Files modified by Patch B-tail

| file | scope of edit |
|---|---|
| `paper/main.tex` | Data Availability section: 2 string replacements (line ~205, ~213); both narrowly target the unresolved values |
| `paper_overleaf/main.tex` | mirror of `paper/main.tex` |
| `paper/audit_reports/consistency_audit.md` | auto-regenerated by `scripts/audit_numbers.py` as a side-effect (header explicitly identifies it as an audit artefact) |

No source-of-truth files (raw QC, processed CSV, stats JSON,
canonical JSON, generator script) were modified in Patch B-tail.

## audit_numbers result

- **exitcode**: 0
- **unresolved count**: **0**
- **Major checks tripped**: **0 / 7**
- **remaining unresolved**: none

## Companion evidence files

- `audit/revision_patchB_ACCEPTED.md`
- `audit/revision_patchB_tail_unresolved_locator.txt`
- `audit/revision_patchB_tail_unresolved_context.md`
- `audit/revision_patchB_tail_audit_numbers.log`
- `audit/revision_patchB_tail_audit_numbers.exitcode`
- `audit/revision_patchB_tail_audit_numbers_summary.md`
- `audit/revision_patchB_tail_numeric_safety.log`
- `audit/revision_patchB_tail_numeric_safety.md`
- `audit/revision_patchB_tail_stale_terms_grep.txt`
- `audit/revision_patchB_tail_stale_terms_classification.md`

## Next step

- **If PASS_PATCH_B_TAIL_UNRESOLVED_ZERO** (the present status):
  **proceed to Patch C** (repository identity cleanup + raw-archive
  tarball + README + manuscript-level verification pass per the
  Phase 5 plan).
- If not: user review is required before Patch C.
