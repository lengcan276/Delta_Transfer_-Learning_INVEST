# Patch D Step 11 — Git cleanliness + tarball safety

## Status: **PASS_GIT_CLEAN_WITH_PRE_EXISTING_ZIP**

## Modified files (by category)

| category | count | files |
|---|---|---|
| MANUSCRIPT_TEXT | 1 | `paper/main.tex` |
| MIRROR_MANUSCRIPT_TEXT | 2 | `paper_overleaf/main.tex`, `paper_overleaf/Table1_invest_candidates.tex` |
| TABLE_OUTPUT | 1 | `results/Table1_invest_candidates.tex` |
| FIGURE_CAPTION_DATA | 2 | `figures/caption_data/Fig0_workflow.json`, `figures/caption_data/Fig4_crosscheck.json` |
| CANONICAL_METRICS | 2 | `results/canonical_metrics.json` (regenerated by `99_emit_canonical.py`), `results/scscc2_extension_n13/stats_n13.json` (regenerated by `build_cross_check_n13.py`) |
| GENERATOR_SCRIPT | 2 | `scripts/99_emit_canonical.py`, `scripts/scscc2_extension/build_cross_check_n13.py` |
| MANUSCRIPT_TEXT (cover letter) | 1 | `reviews/cover_letter.md` |
| AUDIT_REPORT (auto-regenerated) | 1 | `paper/audit_reports/consistency_audit.md` |
| README_OR_DATA_AVAILABILITY | 1 | `README.md` |
| GITIGNORE | 1 | `.gitignore` |
| **modified total** | **14** | |

## Untracked files (by category)

| category | path |
|---|---|
| README_OR_DATA_AVAILABILITY (new) | `DATA_AVAILABILITY.md` |
| AUDIT_REPORT (whole tree, new) | `audit/` |
| AUDIT_SCRIPT (whole tree, new) | `scripts/audit/` |

## Tarball / archive safety

| check | result |
|---|---|
| any raw-output tarball tracked? | **NO** |
| any archive file in working tree? | 1 — `./INVEST_paper_overleaf_n13_v3.zip` (3.1 MB, **pre-existing tracked Overleaf bundle**, not a raw-output tarball, present at HEAD) |
| any new `.tar.gz` / `.tgz` / `.tar.bz2` / `release_assets/*` produced by Patch D? | **NO** |
| `.gitignore` contains tarball / archive patterns? | YES — confirmed at lines 12–16: `release_assets/`, `*.tar.gz`, `*.tar.bz2`, `*.tgz`, `*.zip` |
| `.gitignore` negative test (`test_archive_for_ignore.tar.gz`) | confirmed ignored: `.gitignore:13:*.tar.gz` matched |
| raw calculation files modified? | **NO** — no file under any external `Hz_*/turbo_*/ricc2_*.out` path modified; no raw QC file in the working tree was touched |
| audit temporary dirs exist? | YES — pre-existing `audit/_tmp_phase3_6_patch_workspace/` and `audit/_tmp_repro_repo/`; not introduced by Patch D; deferred to post-Patch-D housekeeping decision |

## Verification commands run

```
git status --short                           → captured to audit/revision_patchD_git_status_after.txt
git diff --stat                              → captured to audit/revision_patchD_diff_stat_after.txt
git diff --name-status                       → captured to audit/revision_patchD_diff_name_status_after.txt
find . ... -name "*.tar.gz" ...              → 0 tarballs found
git ls-files | grep -E "..."                 → only pre-existing INVEST_paper_overleaf_n13_v3.zip
grep -nE "..." .gitignore                    → release_assets/, *.tar.gz, *.tar.bz2, *.tgz, *.zip confirmed
echo "" > test.tar.gz && git check-ignore -v → confirmed ignored
```

## Verdict

- No FAILED_TARBALL_TRACKED — no `.tar.gz` / `.tgz` / `.tar.bz2`
  is tracked or staged. The pre-existing `INVEST_paper_overleaf_n13_v3.zip`
  is an Overleaf paper-bundle artefact from before the revision-mode
  work, not a raw-output tarball.
- No FAILED_RAW_FILE_MODIFIED — no raw calculation file modified.
- No FAILED_OUT_OF_SCOPE_FILE_CHANGE — all 14 modified files are
  inside the Patch A/B/B-tail/C/D allowed-modify whitelist.

Proceed to Step 12.
