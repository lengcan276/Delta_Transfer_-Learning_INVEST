# Patch B — Generator refactor final report

Repository HEAD (pre-revision): `3a72bc9`
Branch: `revision-after-phase5`
Date: 2026-05-20
Canonical repository: `https://github.com/lengcan276/INVEST-n13`

## Status

**PASS_PATCH_B_READY_FOR_REVIEW**

## Files modified

| file | change |
|---|---|
| `scripts/scscc2_extension/build_cross_check_n13.py` | docstring rewritten to declare single-writer scope; output paths switched from external `/home/nudt_cleng/2026/results/scscc2_extension_n13/` to `<release_n13>/results/`; `ci_method` paper-facing label replaced with rule-of-three; `rule_of_three_upper_bound_disagreement_rate` added; `clopper_pearson_note` added flagging CP CI as transparency-only; `repository_canonical` field added |
| `scripts/99_emit_canonical.py` | new `build_scs_cc2_extended_n13()` function added; hooked into `main()` so the block is now generated by `99_emit_canonical.py` rather than being manually JSON-patched in |
| `results/stats_n13.json` | regenerated by refactored script; numerically identical to pre-Patch-B file except for new metadata fields (ci_method text, rule-of-three, transparency note, repository URL) |
| `results/cross_check_n13.csv` | regenerated by refactored script; **byte-identical** to pre-Patch-B file |
| `results/canonical_metrics.json` | regenerated by `99_emit_canonical.py`; `scs_cc2_extended_n13` block now carries paper-facing rule-of-three metadata, transparency-only CP fields, `paper_cited_scope`, `paper_cited_bound`, per-molecule warnings on Hz_NPh21_Cz2 and Hz_POZ1_NPh21_CF31, and `repository_canonical: "https://github.com/lengcan276/INVEST-n13"`; all per-molecule and cohort-level numeric values are unchanged |
| `paper/audit_reports/consistency_audit.md` | auto-regenerated by `scripts/audit_numbers.py` as a side-effect of the completeness check; file header reads "Audit artefact only — `paper/main.tex` is not modified by this script"; mechanical reflection of the Patch-A-added Data Availability text (new unresolved numbers 26.0 and 0.41 surfaced) and of the new `scs_cc2_extended_n13` keys being added to the canonical pool; not manuscript prose |

No other files modified by Patch B.

## What changed

- `canonical_metrics.json` now has a single writer:
  `scripts/99_emit_canonical.py`.
- `build_cross_check_n13.py` only generates `cross_check_n13.csv`
  and `stats_n13.json`.
- `scs_cc2_extended_n13` is assembled by `build_scs_cc2_extended_n13()`
  inside `99_emit_canonical.py`.
- Clopper-Pearson paper-facing interpretation replaced by
  screened-cohort rule-of-three (Clopper-Pearson CI arrays kept as
  transparency-only fields with explicit annotation).
- Repository identity set to `https://github.com/lengcan276/INVEST-n13`
  in both `stats_n13.json` and `canonical_metrics.json`
  (`repository_canonical` field).
- Scientific numerical values unchanged.

## What did not change

- No raw calculation files changed.
- No S1/T1/ΔE_ST scientific values changed.
- No molecule classifications changed.
- No manuscript text changed in Patch B.
- No git commit or push.

## Primary quality gate

Patch B is considered successful only if **single-writer validation
passes, numeric drift check passes, stale metadata grep passes, and
audit_numbers has no unresolved/Major failures.** All four checks pass:

| gate | status | evidence file |
|---|---|---|
| single-writer validation | PASS (chain test reproduced full file from scratch) | `audit/revision_patchB_single_writer_validation.md` |
| numeric drift check | PASS (0 per-molecule drift, 0 cohort drift, 0 non-scs-block drift) | `audit/revision_patchB_numeric_drift_check.md` |
| stale metadata grep | PASS (0 stale Clopper-Pearson paper-facing primary citations) | `audit/revision_patchB_stale_terms_classification.md` |
| audit_numbers completeness | PASS (exitcode 0, 0/7 Major checks tripped) | `audit/revision_patchB_audit_numbers_summary.md` |

## audit_numbers result

- exitcode: **0**
- unresolved: 2 (both are Patch A Data Availability informational
  numbers — `26.0` files-count and `0.41` meV rounding bound — and
  are not Patch B regressions)
- Major checks tripped: **0 / 7**
- Status: completeness check passes; this is a **completeness check,
  not the primary quality gate**.

## Evidence

- `audit/revision_patchB_single_writer_validation.md`
- `audit/revision_patchB_numeric_drift_check.md`
- `audit/revision_patchB_stale_terms_classification.md`
- `audit/revision_patchB_audit_numbers_summary.md`

## Next step

- **User reviews the git diff** of Patch B (3 source files + 3
  regenerated artifact files = 6 paths under the working tree, on
  top of the existing Patch A diff).
- If approved → proceed to Patch set C (repository identity remote
  cleanup + raw-archive tarball + README) and Patch set D
  (manuscript-level verification pass).
- If generator validation failed at any gate → fix the specific
  failing gate in the generator and rerun the chain test; do not
  proceed to C/D until all four gates are PASS.
