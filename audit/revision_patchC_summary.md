# Patch C — Final report

Repository HEAD (pre-revision): `3a72bc9`
Branch: `revision-after-phase5`
Date: 2026-05-21
Canonical repository: `https://github.com/lengcan276/INVEST-n13`

## Status

**PASS_PATCH_C_WITH_USER_DECISION_REQUIRED**

The Patch C deliverables are complete; remaining items are
non-blocking USER_DECISION_REQUIRED follow-ups (see "Remaining
user decisions" below).

## What changed

| area | artefacts |
|---|---|
| repository identity documentation | `audit/revision_patchC_repo_identity_grep.txt`, `audit/revision_patchC_repo_identity_classification.md`, `audit/revision_patchC_git_remotes.txt`, `audit/revision_patchC_remote_guidance.md` |
| legacy snapshot disposition | `audit/revision_patchC_legacy_snapshot_reference_grep.txt`, `audit/revision_patchC_legacy_snapshot_disposition.md` (verdict: LEGACY_REFERENCE_FOUND — root snapshots NOT deleted) |
| raw provenance manifest | `scripts/audit/17_make_raw_provenance_manifest.py` (new), `audit/revision_patchC_raw_provenance_manifest.tsv` (26 rows; sha256 + banner / hostname / scheduler flags), `audit/revision_patchC_raw_provenance_manifest.md` |
| raw archive release plan | `audit/revision_patchC_raw_archive_diagnosis.md` (SCS-CC2 = RELEASE_ASSET_RECOMMENDED; ADC(2) = MANIFEST_ONLY_RECOMMENDED), `audit/revision_patchC_raw_archive_decision.md`, `audit/revision_patchC_raw_archive_release_plan.md` |
| README / DATA_AVAILABILITY | `README.md` (rewritten to canonical INVEST-n13 framing, reproducibility chain, single-writer note, audit-history brief), `DATA_AVAILABILITY.md` (new; canonical URL + processed / canonical / manifest / external-archive / ybsi-pending honest disclosure) |
| `.gitignore` | added `release_assets/`, `*.tar.gz`, `*.tar.bz2`, `*.tgz`, `*.zip` (does NOT ignore existing tracked content; does NOT ignore `audit/*.md`, `audit/*.tsv`, `results/canonical_metrics.json`, or upstream stats / CSV) |
| audit/ inventory | `audit/revision_patchC_audit_file_inventory.txt` (467 files), `audit/revision_patchC_audit_dir_size.txt` (34 MB), `audit/revision_patchC_audit_temp_inventory.txt` (2 `_tmp_*` dirs), `audit/revision_patchC_audit_housekeeping_plan.md` (no disposition decisions; deferred until after Patch D) |
| final checks | `audit/revision_patchC_emit_canonical.log` + exitcode, `audit/revision_patchC_audit_numbers.log` + exitcode, `audit/revision_patchC_stale_terms_grep.txt`, `audit/revision_patchC_final_checks.md` |

## What did not change

- **No raw calculation files modified.**
- **No scientific numerical values changed.**
- **No molecule classifications changed.**
- **No manuscript scientific interpretation changed.** The only
  manuscript-tree file touched is `paper/audit_reports/consistency_audit.md`,
  which is auto-regenerated by `audit_numbers.py` (header explicitly
  identifies it as an audit artefact).
- **No git commit or push.**
- **Patch D not started.**
- **No raw-output tarball committed to git.**
- **No CI/CD configuration added.**
- **No `dependencies` / `requirements` / environment file modified.**
- **No `results/` directory restructured.**
- **No audit file moved, deleted, or reclassified.**

## Remaining user decisions

These are non-blocking and orthogonal to Patch C:

1. **Remove the legacy `fed_old` remote manually**:
   ```bash
   git remote remove fed_old
   ```
   Recommended before any first push of the release. Guidance
   only; Patch C did not execute this.

2. **(Optional) Fix the source-label mislabel at
   `scripts/99_emit_canonical.py:958`** which still names
   `"results/stats_n13.json"` instead of
   `"results/scscc2_extension_n13/stats_n13.json"` in an
   `incon()` source-string. Behaviour-inert; cosmetic.

3. **(Optional) After follow-up #2 lands, `git rm` the legacy
   root snapshots** `results/cross_check_n13.csv` and
   `results/stats_n13.json` (currently held at HEAD content
   per Patch B review-fix decision).

4. **Upload the SCS-CC2 n=13 raw archive as a GitHub Release
   asset** at
   `https://github.com/lengcan276/INVEST-n13/releases`
   (optional Zenodo deposit for DOI citation). Recipe in
   `audit/revision_patchC_raw_archive_decision.md`. Patch C
   produced manifest + checksums + release plan only; the
   actual tarball is built and uploaded externally.

5. **Audit-housekeeping final disposition for `audit/`** —
   what goes into the public commit vs. moves into
   `audit/intermediate/` vs. stays out — deferred until after
   Patch D, per Step 8.

6. **Schedule `ybsi` rsync** for the 15 R1-deploy ADC(2) raw
   outputs and extend the manifest accordingly in a future
   point release.

## Final-check targets (all met)

- `audit_numbers.py` exit 0
- `audit_numbers.py` unresolved = **0**
- `audit_numbers.py` Major checks = **0 / 7**
- No `FedSchNet-ReorgEnergy` in formal files
- No internal `Phase 4` / `Phase 5` / `Step 5.5` in formal files
- No `release_assets/*.tar.gz` tracked or staged
- No raw-output tarball in git working tree
- `.gitignore` protects future tarball additions
- README and DATA_AVAILABILITY both use canonical INVEST-n13 URL

## Next step

**Proceed to Patch D manuscript-level final verification after
user review.** Patch D is responsible for the final
manuscript-level verification pass; it is not started in Patch C.
